[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nlp-dsait4100"
version = "0.1.0"
requires-python = "==3.11.*"

[tool.setuptools]
package-dir = {"" = "."}